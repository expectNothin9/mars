import React from "react";
import styled from "styled-components";

const StyledMessageList = styled.ul`
  flex-grow: 1;
`;

const MessageList = () => <StyledMessageList>MessageList</StyledMessageList>;

export default MessageList;
