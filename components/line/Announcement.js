import React from "react";
import styled from "styled-components";

const StyledAnnouncement = styled.div`
  height: var(--h-unit);
`;

const Announcement = () => (
  <StyledAnnouncement>Announcement</StyledAnnouncement>
);

export default Announcement;
